DATABASE_URL="file:./dev.db"
JWT_SECRET="generate-a-64-char-random-string-here"
NODE_ENV="development"

# Default AI config (used as fallback if DB providers are empty)
AI_PROVIDER="gemini"
AI_BASE_URL="https://generativelanguage.googleapis.com/v1beta/openai/"
AI_API_KEY="your-gemini-key"
AI_MODEL="gemini-2.5-flash"

# SMTP config (for email verification)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT=587
SMTP_USER="your-email@gmail.com"
SMTP_PASS="your-app-password"
SMTP_FROM="Odosian <noreply@odosian.com>"

# Odosian AI Engine (Python service — leave empty to use direct LLM calls only)
ENGINE_URL="http://localhost:8000"

# App config
APP_URL="http://localhost:3000"
RATE_LIMIT_AI=10
RATE_LIMIT_AUTH=5
RATE_LIMIT_GENERAL=60
