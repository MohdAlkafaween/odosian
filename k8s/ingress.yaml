apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: odosian
  namespace: odosian
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.tls: "true"
spec:
  tls:
    - hosts:
        - odosian.local
      secretName: odosian-tls
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: odosian
                port:
                  number: 3000
